const n = 3
const arr = [2, 3, 3]
const color = ['B', 'B', 'B']
let res = 0


// for (let i = 0; i < n; i++) {
//     let startIndex = i + 1
//     while (arr.indexOf(arr[i], startIndex) !== -1) {
//         // 只要能找到和当前相等的数字

//         startIndex = arr.indexOf(arr[i], startIndex) + 1
//     }
// }
console.log(res)